"use strict";
var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var platforms_1 = __importDefault(require("./platforms"));
var platform_1 = require("./platforms/platform");
var formats = {};
var listFormats = platform_1.PlatformFormat.formats;
for (var p in platforms_1.default) {
    var platform = platforms_1.default[p];
    if (platform.extraFormats) {
        listFormats = __spreadArrays(listFormats, [platform.extraFormats]);
    }
}
function default_1(langDefault) {
    listFormats.forEach(function (name) {
        formats[name] = function (text, params, _a, user) {
            var session = (_a === void 0 ? {} : _a).session;
            if (!session) {
                return text;
            }
            var _b = session.message || session, source = _b.source, agent = _b.agent;
            if (source == agent) {
                agent = undefined;
            }
            var p = platforms_1.default[source + (agent ? '-' + agent : '')];
            if (p) {
                var platform = new p(text, session, user);
                if (!platform[name]) {
                    return text;
                }
                platform.defaultLanguage = langDefault;
                return platform[name].apply(platform, params);
            }
            return text;
        };
    });
    return formats;
}
exports.default = default_1;
